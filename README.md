# AI-Powered-Receipt-Extraction-System
The system performs image preprocessing, identifies key fields such as vendor name, transaction date, and total amounts via regex-based pattern matching, and generates comprehensive financial summaries in structured JSON format with integrated confidence-aware reliability scoring.
